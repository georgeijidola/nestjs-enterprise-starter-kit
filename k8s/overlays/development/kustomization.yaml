apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
namespace: development
patches:
  - target:
      kind: Deployment
      name: nestjs-enterprise-starter-kit
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
